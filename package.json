{
  "name": "elysia-on-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Reproduction: @vercel/node breaks TypeScript project references (composite: true)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.3.2"
}
