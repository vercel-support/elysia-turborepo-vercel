{
  "name": "@elysia-on-vercel/api",
  "type": "module",
  "version": "1.0.50",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "tsc -b --force"
  },
  "dependencies": {
    "@elysia-on-vercel/shared": "workspace:*",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
